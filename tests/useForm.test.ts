import { cleanup } from '@testing-library/react'
import { afterEach, describe, expect, test } from 'vitest'

afterEach(() => cleanup())

describe('...', () => {
  test('...', () => {
    expect(1).toBe(1)
  })
})
